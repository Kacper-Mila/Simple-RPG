namespace Simple_RPG.Items;

public class AxeItem : WeaponItem
{
    public AxeItem(string name, string description, int minDamage, int maxDamage) 
        : base(name, description, minDamage, maxDamage)
    {
    }
}